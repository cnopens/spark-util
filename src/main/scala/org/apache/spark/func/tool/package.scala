package org.apache.spark.func

import org.apache.spark.streaming.StreamingContext
import org.apache.spark.rdd.RDD
/**
 * @author LMQ
 * @desc 为此package添加隐式转换的function。方便使用，若有其他的隐式功能，可在此package下面添加并，继承
 */
package object tool extends KafkaImplicittrait{
}